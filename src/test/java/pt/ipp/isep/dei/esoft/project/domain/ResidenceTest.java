package pt.ipp.isep.dei.esoft.project.domain;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class ResidenceTest {
    /*@Test
    public void testGetters() {
        Residence residence = new Residence(200,90, 20000,"https://www.example.com/index.html#date" , new Address("Rua da Serra", "Gaia", "Porto", "12345"), 2, 3, 2, new AvailableEquipment(2,2));

        assertEquals(200, residence.getArea());
        assertEquals(90, residence.getDistanceFromCity());
        assertEquals(20000, residence.getPrice());
        assertEquals("https://www.example.com/index.html#date", residence.getPhotograph());
        assertEquals("Rua da Serra", residence.getAddress().getStreetName());
        assertEquals("Gaia", residence.getAddress().getCityName());
        assertEquals("Porto", residence.getAddress().getDistrict());
        assertEquals("12345", residence.getAddress().getZipCode());
        assertEquals(2, residence.getNumberBedrooms());
        assertEquals(3, residence.getNumberBathrooms());
        assertEquals(2, residence.getNumberParkingSpaces());
        assertEquals(2, residence.getAvailableEquipment().getAirConditioning());
        assertEquals(2, residence.getAvailableEquipment().getCentralHeating());
    }*/
}
