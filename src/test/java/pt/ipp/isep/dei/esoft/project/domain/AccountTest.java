package pt.ipp.isep.dei.esoft.project.domain;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class AccountTest {
   /* @Test
    public void testGetters(){
        Account account = new Account("Fátima", "12345678", "333-22-4444", "user@gmail.com",
                "(333) 333-4444", "password", new Address("rua da serra", "gaia", "porto", "12345"));

        assertEquals("Fátima", account.getName());
        assertEquals("12345678", account.getPassportCardNumber());
        assertEquals("333-22-4444", account.getTaxNumber());
        assertEquals("user@gmail.com", account.getEmailAddress());
        assertEquals("(333) 333-4444", account.getPhoneNumber());
        assertEquals("password", account.getPassword());
        assertEquals("rua da serra", account.getAddress().getStreetName());
        assertEquals("gaia", account.getAddress().getCityName());
        assertEquals("porto", account.getAddress().getDistrict());
        assertEquals("12345", account.getAddress().getZipCode());
    }*/
}
