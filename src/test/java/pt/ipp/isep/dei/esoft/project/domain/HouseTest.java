package pt.ipp.isep.dei.esoft.project.domain;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class HouseTest {
    /*@Test
    public void testGetters(){
        House house = new House(200, 30, 30000,"https://www.example.com/index.html#date", new Address("rua da serra",
                "gaia", "porto", "12345"), 3, 2, 2,
                new AvailableEquipment(1, 1), 1, 1, 1);

        assertEquals(200, house.getArea());
        assertEquals(30, house.getDistanceFromCity());
        assertEquals(30000, house.getPrice());
        assertEquals("https://www.example.com/index.html#date", house.getPhotograph());
        assertEquals("rua da serra", house.getAddress().getStreetName());
        assertEquals("gaia", house.getAddress().getCityName());
        assertEquals("porto", house.getAddress().getDistrict());
        assertEquals("12345", house.getAddress().getZipCode());
        assertEquals(1, house.getAvailableEquipment().getAirConditioning());
        assertEquals(1, house.getAvailableEquipment().getCentralHeating());
        assertEquals(1, house.getExistenceBasement());
        assertEquals(1, house.getExistenceLoft());
        assertEquals(1, house.getSunExposure());
    }*/
}
