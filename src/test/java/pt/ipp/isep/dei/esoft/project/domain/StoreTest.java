package pt.ipp.isep.dei.esoft.project.domain;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class StoreTest {
    /*@Test
    void testStore() {
        Address address = new Address("Rua da Serra", "Gaia", "Porto", "12345");
        Store store = new Store(address, "1", "(555) 555-5555", "store", "user@gmail.com");

        assertEquals(address, store.getAddress());
        assertEquals("store", store.getDesignation());
        assertEquals("1", store.getId());
        assertEquals("user@gmail.com", store.getEmail());
        assertEquals("(555) 555-5555", store.getPhoneNumber());
        assertNull(store.getStoreManager());
    }*/
}
